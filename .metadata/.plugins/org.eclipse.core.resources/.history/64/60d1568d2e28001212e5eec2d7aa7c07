package com.valleskeyp.data;

public interface Films {
	//possibly make an interface that sets the movie to search for
	
	//class that implements this interface takes the movie name then accesses the
	//rotten tomatoes API for the movie's data.  it can return this data to the textView
}
